# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_DEBUG=false

# Self-host escape hatch: set to "true" to bypass the login wall so the UI is
# reachable without a backend. Features that call the API will still fail.
# Leave unset/false in production.
NEXT_PUBLIC_DISABLE_AUTH=false